{
  "name": "@ben-poole/lerna-test",
  "version": "0.0.1",
  "publishConfig": { 
    "registry": "http://localhost:4873"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "audit": "lerna run audit --parallel",
    "publish:release": "lerna version --conventional-commits --yes",
    "publish:package": "lerna publish --yes --cd-version from-git --registry=http://localhost:4873",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}

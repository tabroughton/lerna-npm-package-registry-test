{
  "name": "@ben-poole/lerna-test",
  "version": "0.0.1",
  "publishConfig": { 
    "registry": "http://localhost:4873"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "audit": "lerna run audit --parallel",
    "publish:release": "lerna version --conventional-commits --yes",
    "publish:package": "lerna publish --yes --registry=http://localhost:4873 from-git",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
